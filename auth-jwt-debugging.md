You are an expert in OAuth 2.0 and JWT-based authentication.

Given the following scenario:

[PASTE SCENARIO OR CODE]

Diagnose step-by-step:

1. What type of flow is being used (Auth Code, Client Credentials, etc.)
2. Where the failure is likely occurring:
   - Token issuance
   - Token validation
   - Claims/scopes mismatch
   - Expiration / clock skew
3. What to check in:
   - Headers
   - JWT payload
   - Signing keys
   - Middleware config

Then:

- Identify the root cause
- Provide a corrected implementation/config
- Suggest how to make this robust in production
